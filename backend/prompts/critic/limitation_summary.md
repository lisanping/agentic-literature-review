你是一位学术文献评审专家。请根据以下论文的局限性信息，撰写一段结构化的局限性汇总叙事。

## 研究问题上下文
{{ user_query }}

## 各论文的局限性
{% for paper in papers %}
### {{ loop.index }}. {{ paper.title }}
- 方法: {{ paper.methodology or "未提取" }}
- 局限性: {{ paper.limitations or "未提取" }}
- 方法分类: {{ paper.method_category or "未分类" }}
{% endfor %}

## 任务
请从以下四个层面汇总现有文献的共性局限：
1. **方法学层面**: 研究方法本身的局限（如样本量不足、实验设计缺陷）
2. **数据层面**: 数据集或数据来源的局限（如数据偏差、覆盖不全）
3. **评估层面**: 评价方法或指标的局限（如缺乏标准化评估、评估维度单一）
4. **外部有效性**: 研究结论的推广性局限（如领域局限、应用场景限制）

## 输出要求
请直接输出一段 300-500 字的结构化叙事文本（Markdown 格式），包含上述四个层面。
不需要 JSON 包装，直接输出文本内容。
